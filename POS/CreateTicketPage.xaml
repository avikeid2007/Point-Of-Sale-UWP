<Page
    x:Class="POS.CreateTicketPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:POS"
    xmlns:data="using:POS.Models"

    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:behaviors="using:Microsoft.Toolkit.Uwp.UI.Animations.Behaviors"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:extensions="using:Microsoft.Toolkit.Uwp.UI.Extensions"
    xmlns:animations="using:Microsoft.Toolkit.Uwp.UI.Animations"
    xmlns:userControl="using:POS.UserControls"
    mc:Ignorable="d" Loading="Page_Loading">


    <Page.Resources>
        
        <!--Category GridView -->
        <DataTemplate x:DataType="data:Category" x:Key="CategoryGridDataTemplate" >
            <Grid Height="100" Width="100" Background="Tan" Opacity=".9" >

                <TextBlock Text="{x:Bind name}"  TextWrapping="WrapWholeWords" HorizontalAlignment="Center" VerticalAlignment="Center" Width="Auto"  FontSize="25" />

            </Grid>
        </DataTemplate>
        <!--Item GridView -->
        <DataTemplate x:DataType="data:Item" x:Key="ItemGridDataTemplate" >
            <Grid Height="100" Width="190" Background="{StaticResource ItemBrush}"  >

                <TextBlock Text="{x:Bind name}" TextWrapping="WrapWholeWords" HorizontalAlignment="Center" VerticalAlignment="Center" Width="Auto"  FontSize="20" />

            </Grid>
        </DataTemplate>
        
        <!--Modifier Groups ListView -->
        <DataTemplate x:DataType="data:Modifier" x:Key="ModGroupDataTemplate" >
            <Grid HorizontalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="32"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock VerticalAlignment="Center" FontSize="16" Text="{x:Bind name}" Padding="4"  />
                <Rectangle Canvas.ZIndex="-1" Grid.ColumnSpan="10" Grid.RowSpan="10" Fill="{ThemeResource SystemControlBackgroundAccentBrush}" Opacity=".7" />
            </Grid>

        </DataTemplate>
        <!--Modifier Items ListView -->
        <DataTemplate x:DataType="data:Modifier" x:Key="ModItemDataTemplate" >
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center" Grid.Column="1" FontSize="14" Text="{x:Bind name}"/>
            </Grid>
        </DataTemplate>


        <!--Current Ticket ListView -->
        <DataTemplate x:DataType="data:Item" x:Key="CurrentTicketDataTemplate" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="8*"/>
                    <ColumnDefinition Width="4*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{x:Bind name}"   TextWrapping="Wrap" HorizontalAlignment="Left" Grid.Column="1"   VerticalAlignment="Center" Width="Auto"  FontSize="15" />
                <TextBlock Text="{x:Bind minQuantity}"  HorizontalAlignment="Center"    VerticalAlignment="Center" Width="Auto"  FontSize="15"/>
                <TextBlock Text="{x:Bind price}"        HorizontalAlignment="Right"   Grid.Column="2" VerticalAlignment="Center" Width="Auto" Margin="0,0,10,0"  FontSize="15" />
            </Grid>
        </DataTemplate>
        <!--Item Expanded ListView -->
        <DataTemplate x:DataType="data:Item" x:Key="ItemSelectedDataTemplate" >
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="8*"/>
                    <ColumnDefinition Width="4*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="{x:Bind name}"    TextWrapping="Wrap"     HorizontalAlignment="Left" Grid.Column="1"   VerticalAlignment="Center" Width="Auto"  FontSize="15" />
                <TextBlock Text="{x:Bind minQuantity}"  HorizontalAlignment="Center" Grid.Column="0"  VerticalAlignment="Center" Width="Auto"  FontSize="15" Margin="0,0,10,0" />
                <TextBlock Text="{x:Bind price}"        HorizontalAlignment="Right"   Grid.Column="2" VerticalAlignment="Center" Width="Auto" Margin="0,0,10,0"  FontSize="15" />

                <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.ColumnSpan="10">
                    <Button x:Name="editItem" Content="Edit" Tapped="editItem_Click"/>
                    <Button x:Name="removeItem" Content="Remove" Tapped="removeItem_Tapped" />
                </StackPanel>
            </Grid>
        </DataTemplate>

        <!--Current Ticket Discount ListView -->
        <DataTemplate x:DataType="data:Item" x:Key="CurrentTicketDiscountDataTemplate" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="8*"/>
                    <ColumnDefinition Width="4*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="{x:Bind name}"   TextWrapping="Wrap"      HorizontalAlignment="Left" Grid.Column="1"   VerticalAlignment="Center" Width="Auto"  FontSize="15" />
                <TextBlock Text="{x:Bind minQuantity}"  HorizontalAlignment="Center"   VerticalAlignment="Center" Width="Auto"  FontSize="15"  />
                <TextBlock Text="{x:Bind price}"        HorizontalAlignment="Right"   Grid.Column="2" VerticalAlignment="Center" Width="Auto" Margin="0,0,10,0"  FontSize="15" />
                <TextBlock Text="{x:Bind discount}"        HorizontalAlignment="Right" Grid.Row="1"  Grid.Column="2" VerticalAlignment="Center" Width="Auto" Margin="0,0,10,0"  FontSize="15" />

            </Grid>
        </DataTemplate>
        <!--Item Expanded Discount ListView -->
        <DataTemplate x:DataType="data:Item" x:Key="ItemSelectedDiscountDataTemplate" >
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="8*"/>
                    <ColumnDefinition Width="4*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="{x:Bind name}"   TextWrapping="Wrap"      HorizontalAlignment="Left" Grid.Column="1"   VerticalAlignment="Center" Width="Auto"  FontSize="15" />
                <TextBlock Text="{x:Bind minQuantity}"  HorizontalAlignment="Center"   VerticalAlignment="Center" Width="Auto"  FontSize="15"  />
                <TextBlock Text="{x:Bind price}"        HorizontalAlignment="Right"   Grid.Column="2" VerticalAlignment="Center" Width="Auto" Margin="0,0,10,0"  FontSize="15" />
                <TextBlock Text="{x:Bind discount}"        HorizontalAlignment="Right" Grid.Row="1"  Grid.Column="2" VerticalAlignment="Center" Width="Auto" Margin="0,0,10,0"  FontSize="15" />

                <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.ColumnSpan="4">
                    <Button x:Name="editItem" Content="Edit" Tapped="editItem_Click"/>
                    <Button x:Name="removeItem" Content="Remove" Tapped="removeItem_Tapped" />
                </StackPanel>
            </Grid>
        </DataTemplate>

        
        <!--Current Ticket Modifiers-->
        <DataTemplate x:DataType="data:Item" x:Key="CurrentTicketModDataTemplate" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="8*"/>
                    <ColumnDefinition Width="4*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="{x:Bind name}" TextWrapping="Wrap" HorizontalAlignment="Left" Grid.Column="1" VerticalAlignment="Center" Width="Auto"  FontSize="15" />
                <TextBlock Text="{x:Bind minQuantity}" HorizontalAlignment="Center"  VerticalAlignment="Center" Width="Auto"  FontSize="15"  />
                <TextBlock Text="{x:Bind price}" HorizontalAlignment="Right" Grid.Column="2" VerticalAlignment="Center" Width="Auto" Margin="0,0,10,0"  FontSize="15" />
                <Grid Grid.Row="1" Grid.ColumnSpan="8" Canvas.ZIndex="-1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="7*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="10"/>
                    </Grid.ColumnDefinitions>
                    <Rectangle Grid.ColumnSpan="6"  Fill="Transparent"/>

                    <ListView ItemsSource="{x:Bind modName}" SelectionMode="None"  Grid.Column="1" Canvas.ZIndex="-1" HorizontalAlignment="Stretch" IsItemClickEnabled="False">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="MinHeight" Value="0" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemContainerTransitions>
                            <TransitionCollection/>
                        </ListView.ItemContainerTransitions>
                    </ListView>
                    <ListView ItemsSource="{x:Bind modPrice}" SelectionMode="None" Canvas.ZIndex="-1" HorizontalAlignment="Stretch" Grid.Column="2">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="MinHeight" Value="0" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Right" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemContainerTransitions>
                            <TransitionCollection/>
                        </ListView.ItemContainerTransitions>
                    </ListView>

                </Grid>
                
            </Grid>
        </DataTemplate>
        <!--Current Ticket Modifiers Selected-->
        <DataTemplate x:DataType="data:Item" x:Key="CurrentTicketModSelectedDataTemplate" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="8*"/>
                    <ColumnDefinition Width="4*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="{x:Bind name}" TextWrapping="Wrap" HorizontalAlignment="Left" Grid.Column="1" VerticalAlignment="Center" Width="Auto"  FontSize="15" />
                <TextBlock Text="{x:Bind minQuantity}" HorizontalAlignment="Center"  VerticalAlignment="Center" Width="Auto"  FontSize="15" Margin="0,0,10,0" />
                <TextBlock Text="{x:Bind price}" HorizontalAlignment="Right" Grid.Column="2" VerticalAlignment="Center" Width="Auto" Margin="0,0,10,0"  FontSize="15" />
                <Grid Grid.Row="1" Grid.ColumnSpan="3" Canvas.ZIndex="-1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="7*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="10"/>
                    </Grid.ColumnDefinitions>
                    <ListView ItemsSource="{x:Bind modName}" SelectionMode="None"  Grid.Column="1" HorizontalAlignment="Stretch" IsItemClickEnabled="False">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="MinHeight" Value="0" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemContainerTransitions>
                            <TransitionCollection/>
                        </ListView.ItemContainerTransitions>

                    </ListView>
                    <ListView ItemsSource="{x:Bind modPrice}" SelectionMode="None" HorizontalAlignment="Stretch" Grid.Column="2">
                        <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="MinHeight" Value="0" />
                                    <Setter Property="Padding" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Right" />
                            </Style>
                            </ListView.ItemContainerStyle>
                        <ListView.ItemContainerTransitions>
                            <TransitionCollection/>
                        </ListView.ItemContainerTransitions>

                    </ListView>

                </Grid>
                <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.ColumnSpan="10">
                    <Button x:Name="editItem" Content="Edit" Tapped="editItem_Click"/>
                    <Button x:Name="removeItem" Content="Remove" Tapped="removeItem_Tapped" />
                </StackPanel>

            </Grid>
        </DataTemplate>

        <!--Current Ticket Modifiers Discount-->
        <DataTemplate x:DataType="data:Item" x:Key="CurrentTicketModDiscountDataTemplate" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="8*"/>
                    <ColumnDefinition Width="4*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>

                </Grid.RowDefinitions>
                <TextBlock Text="{x:Bind name}" TextWrapping="Wrap" HorizontalAlignment="Left" Grid.Column="1" VerticalAlignment="Center" Width="Auto"  FontSize="15" />
                <TextBlock Text="{x:Bind minQuantity}" HorizontalAlignment="Center"  VerticalAlignment="Center" Width="Auto"  FontSize="15" />

                <TextBlock Text="{x:Bind price}" HorizontalAlignment="Right" Grid.Column="2" VerticalAlignment="Center" Width="Auto" Margin="0,0,10,0"  FontSize="15" />
                <Grid Grid.Row="1" Grid.ColumnSpan="8" Canvas.ZIndex="-1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="7*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="10"/>
                    </Grid.ColumnDefinitions>
                    <Rectangle Grid.ColumnSpan="6"  Fill="Transparent"/>

                    <ListView ItemsSource="{x:Bind modName}" SelectionMode="None"  Grid.Column="1" Canvas.ZIndex="-1" HorizontalAlignment="Stretch" IsItemClickEnabled="False">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="MinHeight" Value="0" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemContainerTransitions>
                            <TransitionCollection/>
                        </ListView.ItemContainerTransitions>
                    </ListView>
                    <ListView ItemsSource="{x:Bind modPrice}" SelectionMode="None" Canvas.ZIndex="-1" HorizontalAlignment="Stretch" Grid.Column="2">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="MinHeight" Value="0" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Right" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemContainerTransitions>
                            <TransitionCollection/>
                        </ListView.ItemContainerTransitions>
                    </ListView>

                </Grid>
                <TextBlock Text="{x:Bind discount}"        HorizontalAlignment="Right" Grid.Row="2"  Grid.Column="2" VerticalAlignment="Center" Width="Auto" Margin="0,0,10,0"  FontSize="15" />

            </Grid>
        </DataTemplate>
        <!--Current Ticket Modifiers Discount Selected-->
        <DataTemplate x:DataType="data:Item" x:Key="CurrentTicketModDiscountSelectedDataTemplate" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="8*"/>
                    <ColumnDefinition Width="4*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="{x:Bind name}" TextWrapping="Wrap" HorizontalAlignment="Left" Grid.Column="1" VerticalAlignment="Center" Width="Auto"  FontSize="15" />
                <TextBlock Text="{x:Bind minQuantity}" HorizontalAlignment="Center" Grid.Column="0" VerticalAlignment="Center" Width="Auto"  FontSize="15"  />

                <TextBlock Text="{x:Bind price}" HorizontalAlignment="Right" Grid.Column="2" VerticalAlignment="Center" Width="Auto" Margin="0,0,10,0"  FontSize="15" />
                <Grid Grid.Row="1" Grid.ColumnSpan="3" Canvas.ZIndex="-1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="7*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="10"/>
                    </Grid.ColumnDefinitions>
                    <ListView ItemsSource="{x:Bind modName}" SelectionMode="None"  Grid.Column="1" HorizontalAlignment="Stretch" IsItemClickEnabled="False">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="MinHeight" Value="0" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemContainerTransitions>
                            <TransitionCollection/>
                        </ListView.ItemContainerTransitions>

                    </ListView>
                    <ListView ItemsSource="{x:Bind modPrice}" SelectionMode="None" HorizontalAlignment="Stretch" Grid.Column="2">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="MinHeight" Value="0" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Right" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemContainerTransitions>
                            <TransitionCollection/>
                        </ListView.ItemContainerTransitions>
                    </ListView>

                </Grid>

                <TextBlock Text="{x:Bind discount}" HorizontalAlignment="Right" Grid.Row="2"  Grid.Column="2"  VerticalAlignment="Center" Width="Auto" Margin="0,0,10,0"  FontSize="15" />
                <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.ColumnSpan="10">
                    <Button x:Name="editItem" Content="Edit" Tapped="editItem_Click"/>
                    <Button x:Name="removeItem" Content="Remove" Tapped="removeItem_Tapped" />
                </StackPanel>

            </Grid>
        </DataTemplate>



        <!--Filtered Customers ListView -->   
        <DataTemplate x:DataType="data:Customer" x:Key="FilteredCustomerTemplate"  >
            <Grid  HorizontalAlignment="Stretch" RightTapped="Grid_RightTapped_1">

                

                        <FlyoutBase.AttachedFlyout>
                    <MenuFlyout >
                        <MenuFlyoutItem Text="Edit Info" Tapped="MenuFlyoutItem_Tapped"/>
                        <MenuFlyoutItem Text="View Customer History" Tapped="MenuFlyoutItem_Tapped_1"/>
                    </MenuFlyout>
                        </FlyoutBase.AttachedFlyout>

                    <StackPanel  Orientation="Vertical" HorizontalAlignment="Stretch"  >

                    <StackPanel Orientation="Horizontal" Width="1000">
                    <TextBlock Text="{x:Bind full}"   FontSize="20"  Width="10000"/>

                </StackPanel>
                    <TextBlock Text="{x:Bind home}" HorizontalAlignment="Left" VerticalAlignment="Center"  FontSize="15" Width="10000" />
            </StackPanel>
               
            </Grid>
        </DataTemplate>
        
        <!-- ticket history-->
        <DataTemplate x:DataType="data:Ticket" x:Key="TicketIDListViewDataTemplate" >
            <StackPanel >
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{x:Bind ticketID}" Grid.Column="0" HorizontalAlignment="Left" FontSize="18" Margin="3,0,0,0"/>
                    <TextBlock Text="{x:Bind total}" Grid.Column="1" HorizontalAlignment="Left" FontSize="18" Margin="3,0,0,0" />
                    <TextBlock Text="{x:Bind inputDate }" Grid.Column="2" HorizontalAlignment="Left" FontSize="18" Margin="3,0,0,0" />

                </Grid>
                <FlyoutBase.AttachedFlyout>
                    <Flyout>
                        <StackPanel>
                            <TextBlock Text="Hello"/>
                        </StackPanel>
                    </Flyout>
                </FlyoutBase.AttachedFlyout>
            </StackPanel>
        </DataTemplate>
        <!-- ticket history expanded-->
        <DataTemplate x:DataType="data:Ticket" x:Key="TicketHistoryExpandedDataTemplate" >
            <StackPanel >
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="{x:Bind ticketID}" Grid.Column="0" HorizontalAlignment="Left" FontSize="18" Margin="3,0,0,0"/>
                    <TextBlock Text="{x:Bind total}" Grid.Column="1" HorizontalAlignment="Left" FontSize="18" Margin="3,0,0,0" />
                    <TextBlock Text="{x:Bind inputDate }" Grid.Column="2" HorizontalAlignment="Left" FontSize="18" Margin="3,0,0,0" />
                    <StackPanel Grid.Row="1" Grid.ColumnSpan="4" Orientation="Horizontal">
                        <Button Content="Duplicate Ticket" x:Name="dupTicket" Tapped="dupTicket_Tapped" Margin="0,0,5,0" />
                        <Button Content="Quick View" x:Name="quickViewOpen" Tapped="quickViewOpen_Tapped" Margin="0,0,5,0"/>
                        <Button Content="Edit Ticket" Margin="0,0,5,0"/>
                        <Button x:Name="printTicket" Content="Print" Margin="0,0,5,0" Tapped="printTicket_Tapped">
                            <FlyoutBase.AttachedFlyout>
                                <Flyout x:Name="printFlyout">
                                    <StackPanel>
                                        <StackPanel Orientation="Vertical">
                                            <RadioButton Content="Customer Copy" x:Name="selectCustCopy" Checked="selectCustCopy_Checked" />
                                            <RadioButton Content="Store Copy" x:Name="selectStoreCopy" Checked="selectStoreCopy_Checked" />
                                            <RadioButton Content="Invoice" x:Name="selectInvoice" Checked="selectInvoice_Checked" />
                                        </StackPanel>
                                        <Button Content="Confirm" x:Name="confirmConfirm" Tapped="confirmConfirm_Tapped" />
                                    </StackPanel>

                                </Flyout>
                            </FlyoutBase.AttachedFlyout>
                        </Button>
                        <Button Content="Mark as Picked Up" Margin="0,0,5,0"/>

                    </StackPanel>

                </Grid>
                <FlyoutBase.AttachedFlyout>
                    <Flyout>
                        <StackPanel>
                            <TextBlock Text="Hello"/>
                        </StackPanel>
                    </Flyout>
                </FlyoutBase.AttachedFlyout>
            </StackPanel>
        </DataTemplate>

        
        <!--Print Notes-->
        <DataTemplate x:DataType="data:Notes" x:Key="PrintNoteTemplate" >
            <Grid >

                <TextBlock Text="{x:Bind note}" HorizontalAlignment="Stretch"  TextWrapping="Wrap" />
               
            </Grid>
        </DataTemplate>

        <!-- QuickView Notes-->
        <DataTemplate x:DataType="data:Notes" x:Key="quickNoteTemplate" >
            <Grid >
                <TextBlock Text="{x:Bind note}" HorizontalAlignment="Stretch" Margin="0,0,0,0" TextWrapping="Wrap" />
            </Grid>
        </DataTemplate>
        <!--Quick Payment ListView -->
        <DataTemplate x:DataType="data:Payments" x:Key="quickPaymentListTemplate" >
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="{x:Bind amount}" Grid.Column="1" HorizontalAlignment="Right"/>
                <TextBlock Text="{x:Bind type}" Grid.Column="0" HorizontalAlignment="Center"/>
            </Grid>
        </DataTemplate>

        <!--Tax GridView -->
        <DataTemplate x:DataType="data:Tax" x:Key="TaxGridDataTemplate" >
            <Grid Height="60" Width="190" Background="Tan" Opacity=".95">

                <StackPanel Orientation="Vertical" HorizontalAlignment="Left">
                    <TextBlock Text="{x:Bind name}" Margin="5,0,0,0"  HorizontalAlignment="Left" FontSize="25" VerticalAlignment="Center" />
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{x:Bind rate}" HorizontalAlignment="Left" FontSize="15" />
                        <TextBlock Text="%" FontSize="15"/>
                        <TextBlock Text=" when "  Margin="2,0,0,0" FontSize="15"/>
                        <TextBlock Text=" $"  FontSize="15"/>
                        <TextBlock Text="{x:Bind taxAtAmount}" HorizontalAlignment="Left" FontSize="15" />
                        <TextBlock Text=" or above " FontSize="15"/>


                    </StackPanel>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <!--Payment ListView -->
        <DataTemplate x:DataType="data:Payments" x:Key="paymentListTemplate" >
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="{x:Bind amount}" Grid.Column="2" HorizontalAlignment="Center"/>
                <TextBlock Text="{x:Bind till}" Grid.Column="0" HorizontalAlignment="Center"/>
                <TextBlock Text="{x:Bind type}" Grid.Column="1" HorizontalAlignment="Center"/>
            </Grid>
        </DataTemplate>
        <!--Payment ListView Expanded -->
        <DataTemplate x:DataType="data:Payments" x:Key="paymentListSelectedTemplate" >
            <Grid >

                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="{x:Bind amount}" Grid.Column="2" HorizontalAlignment="Center"/>
                    <TextBlock Text="{x:Bind till}" Grid.Column="0" HorizontalAlignment="Center"/>
                    <TextBlock Text="{x:Bind type}" Grid.Column="1" HorizontalAlignment="Center"/>
                    
                </Grid>
                <Button Grid.Row="1" x:Name="removePaymentButton" Content="Remove Payment" Tapped="removePaymentButton_Tapped" HorizontalAlignment="Right"/>


            </Grid>
        </DataTemplate>
        <!--Tills ListView -->
        <DataTemplate x:DataType="data:Till" x:Key="tillListTemplate" >
            <Grid >
                <TextBlock Text="{x:Bind name}" HorizontalAlignment="Center"/>
            </Grid>
        </DataTemplate>
        <!-- Payment Type template -->
        <DataTemplate x:DataType="data:Till" x:Key="paymentTypeTemplate">
            <Grid Height="75" Width="75">
                <TextBlock Text="{x:Bind name}" FontSize="25" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Rectangle Fill="Gray" Opacity=".35"/>
            </Grid>
        </DataTemplate>


        <!--Print Payment ListView -->
        <DataTemplate x:DataType="data:Payments" x:Key="printPaymentListTemplate" >
            <Grid >
                <Grid.ColumnDefinitions>

                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="{x:Bind amount}" Grid.Column="1" HorizontalAlignment="Right"/>
                <TextBlock Text="{x:Bind type}" Grid.Column="0" HorizontalAlignment="Right"/>
            </Grid>
        </DataTemplate>

        <!-- Notes-->
        <DataTemplate x:DataType="data:Notes" x:Key="NoteTemplate" >
            <Grid >

                <TextBlock Text="{x:Bind note}" HorizontalAlignment="Stretch" Margin="0,0,35,0" TextWrapping="Wrap" />
                <Button x:Name="deleteNote" Tapped="deleteNote_Tapped" Content="&#xE107;" FontFamily="Segoe MDL2 Assets" HorizontalAlignment="Right"/>
            </Grid>
        </DataTemplate>

        <!--Discount -->
        <DataTemplate x:DataType="data:Discount" x:Key="DiscountDataTemplate" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{x:Bind type}"   TextWrapping="Wrap" HorizontalAlignment="Center" Grid.Column="0"   VerticalAlignment="Center" Width="Auto"  FontSize="15" />
                <TextBlock Text="{x:Bind amount}"  HorizontalAlignment="Center"  Grid.Column="1"   VerticalAlignment="Center" Width="Auto"  FontSize="15"/>
            </Grid>
        </DataTemplate>
    </Page.Resources>


    <Grid x:Name="grid" Background="Transparent" >

        <Grid.ColumnDefinitions>
            <ColumnDefinition MaxWidth="325" Width="2*" x:Name="custSideColumn"/>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition MaxWidth="325" Width="2*"/>
        </Grid.ColumnDefinitions>

        <userControl:PrintTicket x:Name="printTicketControl" ></userControl:PrintTicket>
        <userControl:PrintInvoice x:Name="printInvoiceControl"></userControl:PrintInvoice>


        <!--Popup item click Menu -->
        <Popup VerticalAlignment="Center"  HorizontalAlignment="Center" Grid.ColumnSpan="3" x:Name="itemClickPopup" IsLightDismissEnabled="True" Width="500" Height="280"   Closed="itemClickPopup_Closed">

            <Popup.ChildTransitions>
                <TransitionCollection>
                    <PopupThemeTransition FromHorizontalOffset="0" FromVerticalOffset="200"/>
                </TransitionCollection>
            </Popup.ChildTransitions>
            <StackPanel Background="Transparent" Padding="12">
                <Grid>
                    <StackPanel Background="Transparent" Padding="12" BorderBrush="Black" BorderThickness="2">
                        <TextBlock Text="Add Item" FontSize="16" Margin="0,0,0,4"/>
                        <Grid >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="190"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="260"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" MaxHeight="30"/>
                                <RowDefinition Height="Auto"  MinHeight="5"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <!-- Items in popup-->

                            <TextBox x:Name="itemQuan" Header="Quantity" FontSize="20"  Grid.Row="0" InputScope="Number" PlaceholderText="1"/>

                            <Grid MaxHeight="305" Grid.Column="2" Grid.RowSpan="3" Grid.Row="0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Grid >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Modifiers" FontSize="20" Grid.Column="0" />
                                    <TextBox x:Name="addModSearch" PlaceholderText="Search" HorizontalAlignment="Stretch" Grid.Column="1" TextChanged="modSearch_TextChanged" />
                                </Grid>
                                <Grid Grid.Row="1" Grid.RowSpan="2" x:Name="modWrapper">
                                <ListView x:Name="itemModListView" ItemClick="itemModListView_ItemClick"   SelectionMode="Multiple"  HorizontalContentAlignment="Stretch" ItemsSource="{x:Bind ModifierGroups}"  ItemTemplate="{StaticResource ModItemDataTemplate}" IsItemClickEnabled="True" >
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="Margin" Value="0"/>
                                            <Setter Property="Padding" Value="0"/>
                                            <Setter Property="MinHeight" Value="0"/>
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />

                                        </Style>
                                    </ListView.ItemContainerStyle>
                                    <ListView.ItemContainerTransitions>
                                        <TransitionCollection/>
                                    </ListView.ItemContainerTransitions>
                                </ListView>
                                </Grid>
                            </Grid>
                            <Button x:Name="addPopupButton" Content="Add" Height="45" Width="100" HorizontalAlignment="Right" Margin="0,10,0,0" Grid.Row="3" VerticalAlignment="Bottom" Click="addPopupButton_Click" />
                            <!-- Items in popup-->

                        </Grid>
                    </StackPanel>
                    <Grid x:Name="addItemColor" Canvas.ZIndex="-1">
                        <interactivity:Interaction.Behaviors>
                            <behaviors:Blur Value="25"/>
                        </interactivity:Interaction.Behaviors>
                        <Grid.Background>
                            <SolidColorBrush Color="Black" Opacity="0.5"/>
                        </Grid.Background>
                    </Grid>

                </Grid>
            </StackPanel>
        </Popup>

        <!--Popup edit item click Menu -->
        <Popup VerticalAlignment="Center" HorizontalAlignment="Center" Grid.ColumnSpan="5" Grid.RowSpan="5" x:Name="editItemClickPopup" IsLightDismissEnabled="True" Width="620" Height="280">

            <Popup.ChildTransitions>
                <TransitionCollection>
                    <PopupThemeTransition FromHorizontalOffset="0" FromVerticalOffset="200"/>
                </TransitionCollection>
            </Popup.ChildTransitions>
            <StackPanel Background="Transparent" Padding="12">
                <Grid>
                    <StackPanel Background="Transparent" Padding="12" BorderBrush="Black" BorderThickness="2">
                        <TextBlock Text="Edit Item" FontSize="16" Margin="0,0,0,4"/>
                        <!-- Items in popup-->
                        <Grid Width="500">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel >
                                <TextBox x:Name="editItemQuan" Header="Quantity:" InputScope="Number" FontSize="20" Grid.Row="0"  PlaceholderText="1"/>
                                <TextBox x:Name="editPrice"  Header="New Price:" InputScope="CurrencyAmount" PlaceholderText="Enter price per unit." FontSize="20"/>
                                <TextBlock  Text="Discount:" FontSize="20"/>
                                <StackPanel Orientation="Horizontal">
                                    <RadioButton x:Name="disAmount" Content="Amount"  Checked="disAmount_Checked" />
                                    <RadioButton x:Name="disPercent" Content="Percentage"  Checked="disAmount_Checked" />
                                </StackPanel>
                                <TextBox FontSize="20" x:Name="editDiscount" InputScope="CurrencyAmount" PlaceholderText="Discount by percentage."  />
                                <ToggleSwitch x:Name="editTaxExempt" Header="Tax:" OnContent="Taxed" OffContent="Tax Exempt" FontSize="20"/>
                                <Button x:Name="saveEditItemPopupButton" Content="Save Changes" HorizontalAlignment="Right" Margin="0,10,0,0" Grid.Row="3" VerticalAlignment="Top" Click="saveEditItemPopupButton_Click" />

                            </StackPanel>

                            <Grid  Grid.Column="2" MaxHeight="350" Grid.Row="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Grid >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Modifiers" FontSize="20" Grid.Column="0" />
                                    <TextBox x:Name="editModSearch" PlaceholderText="Search" HorizontalAlignment="Stretch" Grid.Column="1" TextChanged="editModSearch_TextChanged" />
                                </Grid>
                                <Grid x:Name="editModWrapper" Grid.Row="1">
                                <ListView x:Name="editItemModListView"   HorizontalContentAlignment="Stretch"  ItemsSource="{x:Bind ModifierGroups}"  ItemTemplate="{StaticResource ModItemDataTemplate}" IsItemClickEnabled="True" SelectionMode="Multiple">
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="Margin" Value="0"/>
                                            <Setter Property="Padding" Value="0"/>
                                            <Setter Property="MinHeight" Value="20"/>
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                </ListView>
                                </Grid>
                            </Grid>
                            <!-- Items in popup-->


                        </Grid>
                    </StackPanel>
                    <Grid x:Name="editItemColor" Canvas.ZIndex="-1">
                        <interactivity:Interaction.Behaviors>
                            <behaviors:Blur Value="25"/>
                        </interactivity:Interaction.Behaviors>
                        <Grid.Background>
                            <SolidColorBrush Color="Black" Opacity="0.5"/>
                        </Grid.Background>
                    </Grid>

                </Grid>
            </StackPanel>
        </Popup>

        <!--Popup add customer -->
        <Popup VerticalAlignment="Center" HorizontalAlignment="Center" Grid.ColumnSpan="4" Grid.RowSpan="4" x:Name="addCustomerPopup" IsLightDismissEnabled="True" Width="710" Height="500">

            <Popup.ChildTransitions>
                <TransitionCollection>
                    <PopupThemeTransition FromHorizontalOffset="0" FromVerticalOffset="2000"/>
                </TransitionCollection>
            </Popup.ChildTransitions>
            <StackPanel Background="Transparent" Padding="12">
                <Grid>
                    <StackPanel Background="Transparent" Padding="12" BorderBrush="Black" BorderThickness="2">
                        <TextBlock Text="Add a Customer" FontSize="16" Margin="0,0,0,4"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="220"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="50"/>
                            </Grid.RowDefinitions>
                            <!-- Items in popup-->

                            <StackPanel>

                                <TextBox x:Name="firstCust" Header="First:" FontSize="18"/>
                                <TextBox x:Name="spouseCust" Header="Spouse:" FontSize="18"/>
                                <TextBox x:Name="lastCust" Header="Last:" FontSize="18"/>
                                <TextBox x:Name="companyCust" Header="Company:" FontSize="18"/>


                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="20,0,0,0">

                                <TextBox x:Name="homeCust"  Header="Home Phone:" FontSize="18"/>
                                <TextBox x:Name="cellCust"  Header="Cell Phone:" InputScope="TelephoneNumber" FontSize="18"/>
                                <TextBox x:Name="workCust"  Header="Work Phone:" InputScope="TelephoneNumber" FontSize="18"/>
                                <TextBox x:Name="emailCust" Header="Email:" InputScope="EmailSmtpAddress" FontSize="18"/>

                            </StackPanel>

                            <StackPanel Grid.Column="2" Margin="20,0,0,0">

                                <TextBox x:Name="add1Cust" Header="Address:" FontSize="18"/>
                                <TextBox x:Name="add2Cust" FontSize="18"/>
                                <TextBox x:Name="cityCust" Header="City:" FontSize="18"/>
                                <TextBox x:Name="stateCust" Header="State:" FontSize="18"/>
                                <TextBox x:Name="zipCust" Header="Zip Code:" InputScope="Number" FontSize="18"/>

                            </StackPanel>



                            <Button x:Name="addCustPopup"  Content="Add Customer" HorizontalAlignment="Right" Margin="0,10,0,0" Grid.Row="3" VerticalAlignment="Top" Tapped="addCustPopup_Tapped"/>
                            <!-- Items in popup-->

                        </Grid>
                    </StackPanel>
                    <Grid x:Name="addCustColor" Canvas.ZIndex="-1">
                        <interactivity:Interaction.Behaviors>
                            <behaviors:Blur Value="25"/>
                        </interactivity:Interaction.Behaviors>
                        <Grid.Background>
                            <SolidColorBrush Color="Black" Opacity="0.5"/>
                        </Grid.Background>
                    </Grid>

                </Grid>
            </StackPanel>
        </Popup>

        <!--Popup edit customer -->
        <Popup VerticalAlignment="Center" HorizontalAlignment="Center" Grid.ColumnSpan="4" Grid.RowSpan="4" x:Name="editCustomerPopup" IsLightDismissEnabled="True" Width="710" Height="500">

            <Popup.ChildTransitions>
                <TransitionCollection>
                    <PopupThemeTransition FromHorizontalOffset="0" FromVerticalOffset="2000"/>
                </TransitionCollection>
            </Popup.ChildTransitions>
            <StackPanel Background="Transparent" Padding="12">
                <Grid>

                    <StackPanel Background="Transparent" Padding="12" BorderBrush="Black" BorderThickness="2">
                        <Grid>
                            <TextBlock Text="Edit Customer Info" FontSize="16" Margin="0,0,0,4"/>
                            <Button x:Name="custPopOutButton" Content="&#xE2B4;"  FontFamily="Segoe MDL2 Assets" HorizontalAlignment="Right" FontSize="18" Tapped="custPopOutButton_Tapped"/>
                        </Grid>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="220"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="50"/>
                            </Grid.RowDefinitions>
                            <!-- Items in popup-->

                            <StackPanel>

                                <TextBox x:Name="firstCust1" Header="First:" FontSize="18"/>
                                <TextBox x:Name="spouseCust1" Header="Spouse:" FontSize="18"/>
                                <TextBox x:Name="lastCust1" Header="Last:" FontSize="18"/>
                                <TextBox x:Name="companyCust1" Header="Company:" FontSize="18"/>

                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="20,0,0,0">

                                <TextBox x:Name="homeCust1" extensions:TextBoxMask.Mask="999-999-9999" Header="Home Phone:" InputScope="TelephoneNumber" FontSize="18"/>
                                <TextBox x:Name="cellCust1" extensions:TextBoxMask.Mask="999-999-9999" Header="Cell Phone:" InputScope="TelephoneLocalNumber" FontSize="18"/>
                                <TextBox x:Name="workCust1" extensions:TextBoxMask.Mask="999-999-9999" Header="Work Phone:" InputScope="TelephoneNumber" FontSize="18"/>
                                <TextBox x:Name="emailCust1" Header="Email:" InputScope="EmailSmtpAddress" FontSize="18"/>

                            </StackPanel>

                            <StackPanel Grid.Column="2" Margin="20,0,0,0">

                                <TextBox x:Name="add1Cust1" Header="Address:" FontSize="18"/>
                                <TextBox x:Name="add2Cust1" FontSize="18"/>
                                <TextBox x:Name="cityCust1" Header="City:" FontSize="18"/>
                                <TextBox x:Name="statetCust1" Header="State:" FontSize="18"/>
                                <TextBox x:Name="zipCust1" Header="Zip Code:" extensions:TextBoxMask.Mask="99999" extensions:TextBoxMask.PlaceHolder=" "  InputScope="Number" FontSize="18"/>

                            </StackPanel>


                            <Button x:Name="cancleEdit"  Content="Cancle" Grid.Row="3" Grid.Column="1" HorizontalAlignment="Center" Tapped="cancleEdit_Tapped" />
                            <Button x:Name="editCust"  Content="Save Changes" Grid.Row="3" HorizontalAlignment="Center"  Tapped="editCust_Tapped"/>

                            <!-- Items in popup-->

                        </Grid>
                    </StackPanel>
                    <Grid x:Name="editCustColor" Canvas.ZIndex="-1">
                        <interactivity:Interaction.Behaviors>
                            <behaviors:Blur Value="25" />
                        </interactivity:Interaction.Behaviors>
                        <Grid.Background>
                            <SolidColorBrush Color="Black" Opacity="0.5"/>
                        </Grid.Background>
                    </Grid>

                </Grid>
            </StackPanel>
        </Popup>

        <!--Popup customer history-->
        <Popup VerticalAlignment="Center" HorizontalAlignment="Center" Grid.ColumnSpan="4" Grid.RowSpan="4" x:Name="viewCustHistory" IsLightDismissEnabled="True" Width="600" Height="520">

            <Popup.ChildTransitions>
                <TransitionCollection>
                    <PopupThemeTransition FromHorizontalOffset="0" FromVerticalOffset="2000"/>
                </TransitionCollection>
            </Popup.ChildTransitions>
            <StackPanel Background="Transparent" Padding="12">
                <Grid Width="600">
                    <StackPanel Background="Transparent" Padding="12" BorderBrush="Black" BorderThickness="2">
                        <TextBlock Text="View Customer History" FontSize="16" Margin="0,0,0,4"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>

                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="200"/>

                            </Grid.RowDefinitions>
                            <!-- Items in popup-->

                            <TextBlock  Text="Ticket Number"/>
                            <TextBlock Text="Total" Grid.Column="1"/>
                            <TextBlock Text="Date" Grid.Column="2"/>

                            <ScrollViewer Grid.Row="1" Grid.ColumnSpan="4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>

                                    </Grid.ColumnDefinitions>
                                    <ListView x:Name="ticketIDHistory" Grid.Row="2" Grid.ColumnSpan="3" ItemsSource="{x:Bind CustHistory}" SelectionChanged="ticketIDHistory_SelectionChanged" ScrollViewer.VerticalScrollMode="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.IsVerticalRailEnabled="False" ItemTemplate="{StaticResource TicketIDListViewDataTemplate}" IsItemClickEnabled="True">
                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                            </Style>
                                        </ListView.ItemContainerStyle>
                                    </ListView>
                                </Grid>
                            </ScrollViewer>





                            <!-- Items in popup-->

                        </Grid>
                    </StackPanel>
                    <Grid x:Name="ticketHistColor"  Canvas.ZIndex="-1">
                        <interactivity:Interaction.Behaviors>
                            <behaviors:Blur  Value="25"/>
                        </interactivity:Interaction.Behaviors>
                        <Grid.Background>
                            <SolidColorBrush Color="Black" Opacity="0.5"/>
                        </Grid.Background>
                    </Grid>

                </Grid>
            </StackPanel>
        </Popup>

        <!-- Quick View popup-->
        <Popup VerticalAlignment="Center" HorizontalAlignment="Center" Grid.ColumnSpan="4" Grid.RowSpan="4" x:Name="quickViewPopUp" IsLightDismissEnabled="True" Width="375" Height="530">

            <Popup.ChildTransitions>
                <TransitionCollection>
                    <PopupThemeTransition FromHorizontalOffset="0" FromVerticalOffset="2000"/>
                </TransitionCollection>
            </Popup.ChildTransitions>
            <StackPanel Background="Transparent" Padding="12">
                <Grid Width="375">
                    <ProgressRing x:Name="progress2" HorizontalAlignment="Center"  Width="100" Height="100" VerticalAlignment="Center" Visibility="Collapsed" IsActive="False" Grid.ColumnSpan="10" Canvas.ZIndex="30"/>
                    <Rectangle x:Name="progressTint2" Visibility="Collapsed"  Opacity=".5" Grid.ColumnSpan="10" Fill="Black" Canvas.ZIndex="29"/>
                    <StackPanel Background="Transparent" Padding="12" BorderBrush="Black" BorderThickness="2">
                        <TextBlock Text="Quick View" FontSize="16" Margin="0,0,0,4"/>

                        <Grid>


                            <!-- Items in popup-->

                            <Grid>
                                <Grid.RowDefinitions>

                                    <RowDefinition Height="530"/>
                                </Grid.RowDefinitions>
                                <ScrollViewer Grid.Row="1" Grid.ColumnSpan="4" VerticalScrollBarVisibility="Auto">
                                    <StackPanel>
                                        <StackPanel>
                                            <TextBlock  Text="Ticket Number" x:Name="quickViewTicketID" HorizontalAlignment="Center"/>
                                            <TextBlock Text="Customer Name" x:Name="quickViewName" HorizontalAlignment="Center" Grid.Column="1"/>
                                            <TextBlock Text="Customer Phone" x:Name="quickViewPhone" HorizontalAlignment="Center" Grid.Column="2"/>
                                        </StackPanel>

                                        <TextBlock x:Name="quickViewInputDate" Margin="0,8,0,8"/>

                                        <ListView x:Name="quickViewListView" Grid.Row="1" ItemsSource="{x:Bind QuickViewTicket}" LayoutUpdated="quickViewListView_LayoutUpdated" SelectionMode="None" ItemTemplate="{StaticResource CurrentTicketDataTemplate}" Background="Transparent" ScrollViewer.VerticalScrollMode="Disabled" ScrollViewer.HorizontalScrollMode="Disabled" >
                                            <ListView.ItemContainerStyle>
                                                <Style TargetType="ListViewItem">
                                                    <Setter Property="Margin" Value="0"/>
                                                    <Setter Property="Padding" Value="0"/>
                                                    <Setter Property="MinHeight" Value="20" />
                                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                </Style>
                                            </ListView.ItemContainerStyle>
                                        </ListView>

                                        <Grid  Grid.Row="2" Margin="0,8,0,0" >
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="10"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="Tax: " HorizontalAlignment="Right" Grid.Column="1"/>
                                            <TextBlock x:Name="ticketViewTax" HorizontalAlignment="Right" Grid.Column="2"/>
                                            <TextBlock Text="Total: " HorizontalAlignment="Right" Grid.Column="1" Grid.Row="1"/>
                                            <TextBlock x:Name="ticketViewTotal" HorizontalAlignment="Right" Grid.Column="2" Grid.Row="1"/>

                                            <TextBlock x:Name="quickTermsTitle" Text="Tender:" Margin ="0,8,0,0" Grid.Row="2"/>
                                            <ListView ItemsSource="{x:Bind QuickPayments}" Grid.Row="3" Grid.ColumnSpan="3" ItemTemplate="{StaticResource quickPaymentListTemplate}" SelectionMode="None" Background="Transparent" ScrollViewer.VerticalScrollMode="Disabled" ScrollViewer.HorizontalScrollMode="Disabled" >
                                                <ListView.ItemContainerStyle>
                                                    <Style TargetType="ListViewItem">
                                                        <Setter Property="Margin" Value="0"/>
                                                        <Setter Property="Padding" Value="0"/>
                                                        <Setter Property="MinHeight" Value="20" />
                                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                    </Style>
                                                </ListView.ItemContainerStyle>
                                            </ListView>
                                        </Grid>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock x:Name="quickReadyChangeAmountTitle" Text="Change Given: "/>
                                            <TextBlock x:Name="quickViewChangeAmount"/>
                                        </StackPanel>
                                        <TextBlock x:Name="quickNoteTitle" Text="Notes:" Margin ="0,8,0,0"/>
                                        <ListView x:Name="quickListNotes" Grid.Row="1"  ItemsSource="{x:Bind QuickTicketNotes}"  SelectionMode="None" ItemTemplate="{StaticResource quickNoteTemplate}" Background="Transparent" ScrollViewer.VerticalScrollMode="Disabled" ScrollViewer.HorizontalScrollMode="Disabled" >
                                            <ListView.ItemContainerStyle>
                                                <Style TargetType="ListViewItem">
                                                    <Setter Property="Margin" Value="0"/>
                                                    <Setter Property="Padding" Value="0"/>
                                                    <Setter Property="MinHeight" Value="20" />
                                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                </Style>
                                            </ListView.ItemContainerStyle>
                                        </ListView>


                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock x:Name="quickReadyDateTitle" Text="Ready Date: "/>
                                            <TextBlock x:Name="quickViewReadyDate"/>
                                        </StackPanel>



                                    </StackPanel>
                                </ScrollViewer>
                            </Grid>

                            <!-- Items in popup-->

                        </Grid>
                    </StackPanel>
                    <Grid  x:Name="quickViewColor" Canvas.ZIndex="-1">
                        <interactivity:Interaction.Behaviors>
                            <behaviors:Blur  Value="25" />
                        </interactivity:Interaction.Behaviors>
                        <Grid.Background>
                            <SolidColorBrush Color="Black" Opacity="0.5"/>
                        </Grid.Background>
                    </Grid>

                </Grid>
            </StackPanel>
        </Popup>

        <!--Add Cust Item Popup -->
        <Popup VerticalAlignment="Center" HorizontalAlignment="Center" Grid.RowSpan="4" Grid.ColumnSpan="6" Grid.Row="1" x:Name="addCustItemPopUp" IsLightDismissEnabled="True" Width="750" Height="320" >
            <Popup.ChildTransitions>
                <TransitionCollection>
                    <PopupThemeTransition FromHorizontalOffset="0" FromVerticalOffset="200"/>
                </TransitionCollection>
            </Popup.ChildTransitions>

            <StackPanel Background="Transparent" Padding="12" >
                <Grid>
                    <StackPanel Padding="12" BorderBrush="Black" BorderThickness="2">
                        <TextBlock Text="Add Item" FontSize="16" Margin="0,0,0,4"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="320"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="390"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="70"/>
                                <RowDefinition Height="70"/>
                                <RowDefinition Height="0"/>
                                <RowDefinition Height="0"/>
                                <RowDefinition Height="70"/>
                                <RowDefinition Height="50"/>

                            </Grid.RowDefinitions>
                            <!-- Items in popup-->

                            <TextBox x:Name="itemName" Header="Name" PlaceholderText="Enter a name"  FontSize="18" Grid.Row="0"  />

                            <TextBox x:Name="price" PlaceholderText="Enter a price to charge customer" InputScope="CurrencyAmount"  Header="Price" FontSize="18" Grid.Row="1" />
                            <TextBox x:Name="itemCost" Header="Cost" PlaceholderText="Enter cost to produce item" InputScope="CurrencyAmount" FontSize="18" Grid.Row="4" />

                            <Button x:Name="addCustItemPopUpButton" Content="Save" HorizontalAlignment="Right" Background="Blue" Margin="0,10,0,0" Grid.Row="6" VerticalAlignment="Top" Tapped="addCustItemPopUpButton_Tapped"/>


                            <Grid Grid.Column="2"  Grid.RowSpan="6">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>


                                <TextBlock Text="Tax" Grid.Row="0" FontSize="20"/>
                                <GridView x:Name="addTaxGridView"  Grid.Row="1"  SelectionMode="Multiple" IsItemClickEnabled="True" />
                                <TextBlock Text="Modifiers" Grid.Row="2" FontSize="20"/>
                                <Grid x:Name="customItemModWrapper" Grid.Row="3" >
                                    <ListView x:Name="addCustomModListView" ItemClick="itemModListView_ItemClick"   SelectionMode="Multiple"  HorizontalContentAlignment="Stretch" ItemsSource="{x:Bind ModifierGroups}"  ItemTemplate="{StaticResource ModItemDataTemplate}" IsItemClickEnabled="True" >
                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Margin" Value="0"/>
                                                <Setter Property="Padding" Value="0"/>
                                                <Setter Property="MinHeight" Value="0"/>
                                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />

                                            </Style>
                                        </ListView.ItemContainerStyle>
                                        <ListView.ItemContainerTransitions>
                                            <TransitionCollection/>
                                        </ListView.ItemContainerTransitions>
                                    </ListView>
                                </Grid>

                            </Grid>


                            <!-- Items in popup-->

                        </Grid>

                    </StackPanel>
                    <Grid x:Name="custItemColor"  Canvas.ZIndex="-1" Grid.ColumnSpan="10" Grid.RowSpan="10">
                        <interactivity:Interaction.Behaviors>
                            <behaviors:Blur Value="25"/>
                        </interactivity:Interaction.Behaviors>
                        <Grid.Background>
                            <SolidColorBrush Color="Black" Opacity="0.5"/>
                        </Grid.Background>
                    </Grid>
                </Grid>
            </StackPanel>


        </Popup>

        <!--Change MultiDrawer Popup-->
        <Popup VerticalAlignment="Center" HorizontalAlignment="Center" Grid.ColumnSpan="4" Grid.RowSpan="4" x:Name="changeMultiDrawerPopup"  Width="600" Height="520">

            <Popup.ChildTransitions>
                <TransitionCollection>
                    <PopupThemeTransition FromHorizontalOffset="0" FromVerticalOffset="2000"/>
                </TransitionCollection>
            </Popup.ChildTransitions>
            <StackPanel Background="Transparent" Padding="12">
                <Grid Width="600">
                    <StackPanel Background="Transparent" Padding="12" BorderBrush="Black" BorderThickness="2">
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>

                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="350"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <!-- Items in popup-->
                            <TextBlock Text="Select Drawer to Take Change From" FontSize="20" Grid.ColumnSpan="2"/>
                            <ListView ItemsSource="{x:Bind Tills}" x:Name="multiDrawerSelect" HorizontalContentAlignment="Stretch" ItemTemplate="{StaticResource tillListTemplate}" HorizontalAlignment="Stretch" Grid.Row="1" BorderBrush="Gray" BorderThickness="0,0,1,0" />
                            <Grid Grid.Row="1" Grid.Column="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="Change Due:" FontSize="55" HorizontalAlignment="Center" Margin="0,35,0,0"/>
                                <TextBlock x:Name="changeMultiTextBlock" Grid.Row="1" FontSize="55" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Grid>

                            <StackPanel Grid.Column="0"  Grid.ColumnSpan="2"  Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" >
                                <Button x:Name="cancelMultiDrawer" Content="Cancel" Tapped="cancelMutliDrawer_Tapped" Margin="0,0,150,0" />
                                <Button x:Name="submitMultiDrawer" Content="Give Change &amp; Submit Ticket" Tapped="submitMultiDrawer_Tapped"/>
                            </StackPanel>




                            <!-- Items in popup-->

                        </Grid>
                    </StackPanel>
                    <Grid x:Name="changeMultiDrawerColor"  Canvas.ZIndex="-1">
                        <interactivity:Interaction.Behaviors>
                            <behaviors:Blur  Value="30"/>
                        </interactivity:Interaction.Behaviors>
                        <Grid.Background>
                            <SolidColorBrush Color="Black" Opacity="0.8"/>
                        </Grid.Background>
                    </Grid>

                </Grid>
            </StackPanel>
        </Popup>

        <!--Change Popup-->
        <Popup VerticalAlignment="Center" HorizontalAlignment="Center" Grid.ColumnSpan="4" Grid.RowSpan="4" x:Name="changePopup"  Width="600" Height="520">

            <Popup.ChildTransitions>
                <TransitionCollection>
                    <PopupThemeTransition FromHorizontalOffset="0" FromVerticalOffset="2000"/>
                </TransitionCollection>
            </Popup.ChildTransitions>
            <StackPanel Background="Transparent" Padding="12">
                <Grid Width="600">
                    <StackPanel Background="Transparent" Padding="12" BorderBrush="Black" BorderThickness="2">

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="350"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <!-- Items in popup-->                            
                            <Grid  Grid.Column="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock x:Name="changeAmountTitle"  Text="Change Due:" FontSize="55" HorizontalAlignment="Center" Margin="0,35,0,0"/>
                                <TextBlock x:Name="changeTextBlock" Grid.Row="1" FontSize="55" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Grid>

                            <StackPanel Grid.Column="0"  Grid.ColumnSpan="2"  Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" >
                                <Button x:Name="cancelChange" Content="Cancel" Margin="0,0,150,0" Tapped="cancelChange_Click" />
                                <Button x:Name="submitChange"  Content="Give Change &amp; Submit Ticket" Tapped="submitChange_Tapped" />
                            </StackPanel>

                            <!-- Items in popup-->

                        </Grid>
                    </StackPanel>
                    <Grid x:Name="changeColor"  Canvas.ZIndex="-1">
                        <interactivity:Interaction.Behaviors>
                            <behaviors:Blur  Value="30"/>
                        </interactivity:Interaction.Behaviors>
                        <Grid.Background>
                            <SolidColorBrush Color="Black" Opacity="0.8"/>
                        </Grid.Background>
                    </Grid>

                </Grid>
            </StackPanel>
        </Popup>
        <ProgressRing x:Name="progress1" HorizontalAlignment="Center"  Width="100" Height="100" VerticalAlignment="Center" Visibility="Collapsed" IsActive="False" Grid.ColumnSpan="10" Canvas.ZIndex="30"/>
        <Rectangle x:Name="progressTint" Visibility="Collapsed"  Opacity=".5" Grid.ColumnSpan="10" Fill="Black" Canvas.ZIndex="29"/>
        



        <!--Customer side-->
        <Grid x:Name="custSideGrid" Grid.Column="0" BorderBrush="Black" BorderThickness="0,0,2,0" >


            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="34" x:Name="expandColumn"/>
                </Grid.ColumnDefinitions>




                <!--Customer Table tabs -->
                <StackPanel Orientation="Horizontal">

                    <StackPanel.Background>
                        <SolidColorBrush Color="Gray" Opacity=".6"/>
                    </StackPanel.Background>

                    <Button x:Name="selectCustList"  Content="Customer" Background="Transparent"/>
                    <Button x:Name="selectTableList" Click="selectTableList_Click"  Content="Table" Background="Transparent" VerticalAlignment="Top"/>

                </StackPanel>
                <Button x:Name="collapseCustomer" Grid.Column="0"  Content="&#xE00E;" FontFamily="Segoe MDL2 Assets" Background="Transparent" HorizontalAlignment="Right" Tapped="collapseCustomer_Tapped" />
                <Button x:Name="openCustomer" Grid.Column="1"  Content="&#xE00F;" FontFamily="Segoe MDL2 Assets" Background="Transparent" HorizontalAlignment="Right" Tapped="openCustomer_Tapped"/>



                <!--Search area -->
                <Grid Grid.Row="1">
                    <TextBox x:Name="searchCritiria" TextWrapping="Wrap"   Margin="0,0,86,0" TextChanged="searchCritiria_TextChanged"/>

                    <ComboBox x:Name="searchType" SelectedIndex="1" HorizontalAlignment="Right" Margin="0,0,0,0" >
                        <ComboBoxItem x:Name="CustomerNameType" Content="Name"/>
                        <ComboBoxItem x:Name="CustomerPhoneType" Content="Phone"/>
                    </ComboBox>

                </Grid>

                <ListView x:Name="searchResults" Grid.Row="2" ItemsSource="{x:Bind FilteredCustomer}" ItemTemplate="{StaticResource FilteredCustomerTemplate}" IsItemClickEnabled="True" ItemClick="searchResults_ItemClick">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemContainerTransitions>
                        <TransitionCollection/>
                    </ListView.ItemContainerTransitions>
                </ListView>
                <!--add customer -->
                <Button x:Name="addCustomer" Content="Add Customer" Grid.Row="3"  HorizontalAlignment="Center" Margin="0,0,0,20" FontSize="20" Click="addCustomer_Click"/>


            </Grid>
            <!--blur -->
            <Grid  Canvas.ZIndex="-1" x:Name="custBackColor">
                <interactivity:Interaction.Behaviors>
                    <behaviors:Blur  Value="25" />
                </interactivity:Interaction.Behaviors>
                <Grid.Background>
                    <SolidColorBrush Color="Black" Opacity="0.5"/>
                </Grid.Background>
            </Grid>



        </Grid>
        <!--Center-->
        <Grid Grid.Column="1"  x:Name="itemsGrid" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="95" MaxWidth="108"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="4*" />
            </Grid.ColumnDefinitions>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <GridView x:Name="catGridView" HorizontalAlignment="Center" Grid.Column="0" ItemsSource="{x:Bind Categories}" ItemTemplate="{StaticResource CategoryGridDataTemplate}" IsItemClickEnabled="True" ItemClick="catGridView_ItemClick" />
                <Button x:Name="catBack" Grid.Row="1" Tapped="catBack_Tapped" Content="&#xE00E;" FontFamily="Segoe MDL2 Assets" HorizontalAlignment="Stretch" Visibility="Collapsed" />
            </Grid>
                <!--Items -->
            <Grid Grid.Column="1"  >

                <Grid.Background>
                    <SolidColorBrush Color="Black" Opacity="0.5"/>
                </Grid.Background>
            </Grid>

            <GridView x:Name="itemGridView" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="2" IsItemClickEnabled="True" ItemsSource="{x:Bind Items}" ItemTemplate="{StaticResource ItemGridDataTemplate}" ItemClick="itemGridView_ItemClick" />
            <Button x:Name="addCustomItem" Content="&#xE109;" FontFamily="Segoe MDL2 Assets" FontSize="18" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="7,0,7,7" Tapped="addCustomItem_Tapped" />
            <Button x:Name="openCalc" Content="&#xE1D0;" FontFamily="Segoe MDL2 Assets" FontSize="20" Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="7,0,7,7" Tapped="openCalc_Tapped" />

        </Grid>


        <!--Ticket side-->
        <Grid Grid.Column="2">


            <Grid  BorderBrush="Black" BorderThickness="2,0,0,0" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="75"/>
                </Grid.RowDefinitions>
                <!-- Customer Info-->
                
                <StackPanel Margin="0,5,0,5" HorizontalAlignment="Center"  Grid.Column="2">
                        <TextBlock x:Name="ticketNumber" HorizontalAlignment="Center" Text="0000000"/>
                     <Grid x:Name="custCurrentTicketHeader"  RightTapped="custCurrentTicketHeader_RightTapped">
                        <StackPanel>
                            <TextBlock x:Name="customerFullName" HorizontalAlignment="Center" />
                            <TextBlock x:Name="CustomerPhoneNumber" HorizontalAlignment="Center" />
                        </StackPanel>
                        <FlyoutBase.AttachedFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Text="Remove From Ticket" x:Name="removeCustTicket" Tapped="removeCustTicket_Tapped" />
                                <MenuFlyoutItem Text="Edit Info" x:Name="editCustTicketHeader" Tapped="editCustTicketHeader_Tapped"/>
                                <MenuFlyoutItem Text="View Customer History" x:Name="viewCustHistTicketHeader" Tapped="viewCustHistTicketHeader_Tapped" />
                            </MenuFlyout>
                        </FlyoutBase.AttachedFlyout>
                    </Grid>
                </StackPanel>


                
                <ListView x:Name="ticketListBox" Grid.Row="1"  IsItemClickEnabled="True" LayoutUpdated="ticketListBox_LayoutUpdated" ItemsSource="{x:Bind CurrentTicket}" ItemTemplate="{StaticResource CurrentTicketDataTemplate}" Background="Transparent" SelectionChanged="ticketListBox_SelectionChanged">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="MinHeight" Value="20" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsStackPanel ItemsUpdatingScrollMode="KeepLastItemInView" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                </ListView>


                <Grid Grid.Row="2">
                    <Grid.RowDefinitions >
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>

                    </Grid.ColumnDefinitions>

                    <Button x:Name="addNote" Content="&#xE109;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Height="30" Tapped="addNote_Tapped"/>
                    <TextBox x:Name="note" HorizontalAlignment="Stretch"  Grid.Column="1" PlaceholderText="Add a note." />
                    <ListView x:Name="noteListView"  MaxHeight="125" Grid.Row="1" Grid.ColumnSpan="2" ItemsSource="{x:Bind ticketNotes}" ItemTemplate="{StaticResource NoteTemplate}" IsItemClickEnabled="True" >
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="BorderBrush" Value="White"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </ListView.ItemContainerStyle>
                    </ListView>
                    <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.ColumnSpan="3">
                        <TextBlock Text="Item Count: "/>
                        <TextBlock x:Name="itemCount" Text="0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.ColumnSpan="3">
                        <TextBlock Text="Tax: "/>
                        <TextBlock x:Name="ticketTax" Text="0.00"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Grid.Row="4" Grid.ColumnSpan="3" >
                        <TextBlock Text="Total: "/>
                        <TextBlock x:Name="ticketTotal" Text="0.00"/>
                    </StackPanel>

                </Grid>




                <Button x:Name="finishTicket" Content="Finalize Ticket" Grid.Row="3" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,20" FontSize="20" Click="finishTicket_Click"/>
            </Grid>
            <Grid  Canvas.ZIndex="-1" x:Name="ticketSideColor">
                <interactivity:Interaction.Behaviors>
                    <behaviors:Blur Value="25" />
                </interactivity:Interaction.Behaviors>
                <Grid.Background>
                    <SolidColorBrush Color="Black" Opacity="0.5"  />
                </Grid.Background>
            </Grid>
        </Grid>

        <!--Finish Ticket-->
        <Grid  x:Name="finishTicketGrid" Grid.Column="0" Canvas.ZIndex="-1" Grid.ColumnSpan="2" Opacity="0" Background="Transparent" >
            
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Finish Ticket" FontSize="16" Margin="0,0,0,4" Padding="10"/>
                <ListView Grid.ColumnSpan="10" Grid.Row="1" SelectionMode="None">
                    <!-- List of payments added-->
                    <ListViewItem>
                    <Grid x:Name="paymentGrid" Background="Black" Padding="10" Width="260" Height="400">
                        <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="3*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"  />
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="Terms" Grid.Column="1" HorizontalAlignment="Center" />
                            <TextBlock Text="Amount" Grid.Column="2" HorizontalAlignment="Center"/>
                            <TextBlock Text="Drawer" HorizontalAlignment="Center"/>
                            <ListView x:Name="paymentListView" Grid.ColumnSpan="10" BorderBrush="Gray" BorderThickness="0,1,0,1"  Grid.Row="1" ItemsSource="{x:Bind TicketPayments}" HorizontalContentAlignment="Stretch" ItemTemplate="{StaticResource paymentListTemplate}" HorizontalAlignment="Stretch" IsItemClickEnabled="True" SelectionChanged="paymentListView_SelectionChanged" >
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem" >
                                        <Setter Property="Margin" Value="0"/>
                                        <Setter Property="Padding" Value="0"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    </Style>
                                </ListView.ItemContainerStyle>
                            </ListView>
                            <Grid Grid.Row="2" Grid.ColumnSpan="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="70"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Total:"/>
                                    <TextBlock Text="Total Due:"/>
                                </StackPanel>
                            <StackPanel Grid.Column="1" HorizontalAlignment="Right" Grid.Row="2" >
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="$"/>
                                    <TextBlock x:Name="totalFinish"  Text="$0.00" HorizontalAlignment="Right"/>
                                </StackPanel>
                                <TextBlock x:Name="amountDue" Text="$0.00" HorizontalAlignment="Right"/>
                            </StackPanel>
                            </Grid>
                         </Grid>
                    </ListViewItem>
                    <!-- Select Till and submit payment -->
                    <ListViewItem>
                        <Grid  Grid.Column="1" Height="400">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <GridView x:Name="paymentTypeGridView" ItemTemplate="{StaticResource paymentTypeTemplate}" ItemsSource="{x:Bind PaymentTypes}" SelectionChanged="paymentTypeGridView_SelectionChanged"  VerticalAlignment="Top" SelectionMode="Single" Grid.Column="1">
                                
                            </GridView>
                            <ListView x:Name="drawerListView" ItemsSource="{x:Bind Tills}" ItemTemplate="{StaticResource tillListTemplate}" Grid.Row="1"/>
                            <Grid  Grid.Row="2" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="payAmount" InputScope="CurrencyAmount" Header="Payment Amount"   HorizontalAlignment="Stretch"/>
                                <Button x:Name="submitPayment" Content="Submit" Grid.Column="1" VerticalAlignment="Bottom" Tapped="submitPayment_Tapped" />
                            </Grid>

                        </Grid>
                    </ListViewItem>
                    <!-- Ready Date -->
                    <ListBoxItem>
                        <StackPanel Grid.Column="2" >
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Ready Date:" FontSize="20"/>
                                <TextBlock FontSize="20" x:Name="selectedDate" HorizontalAlignment="Center"/>
                            </StackPanel>

                        <CalendarView x:Name="readyDate"  SelectionMode="Single" SelectedDatesChanged="readyDate_SelectedDatesChanged"/>
                            <StackPanel  Orientation="Horizontal">
                                <CheckBox x:Name="checkReadyTime" Content="" MinWidth="8" Width="23" />
                                <TimePicker x:Name="readyTime" TimeChanged="readyTime_TimeChanged"  HorizontalAlignment="Left"/>
                            </StackPanel>
                        </StackPanel>
                    </ListBoxItem>

                    <!-- Discounts -->
                    <ListBoxItem>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="300px"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Text="Discounts" FontSize="20" />
                            
                            <Grid Grid.Row="1" Padding="10" x:Name="discountsBackground"  Background="Black" >
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Type" HorizontalAlignment="Center" Margin="0,3,0,0" />
                                <TextBlock Text="Value" Grid.Column="1"  HorizontalAlignment="Center" />
                                <ListView Grid.Row="1" Grid.ColumnSpan="3" x:Name="discountsListView"  BorderBrush="Gray" BorderThickness="0,1,0,0" ItemsSource="{x:Bind TicketDiscounts}" ItemTemplate="{StaticResource DiscountDataTemplate}" >
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="Margin" Value="0"/>
                                            <Setter Property="Padding" Value="0"/>
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                </ListView>
                            </Grid>
                            <StackPanel Grid.Row="2" Orientation="Horizontal"  >
                                <RadioButton Content="Amount" x:Name="ticketDiscountAmount" Checked="ticketDiscountAmount_Checked" IsChecked="True" />
                                <RadioButton Content="Percentage" x:Name="ticketDiscountPercentage" Checked="ticketDiscountPercentage_Checked" />
                            </StackPanel>
                            <StackPanel Grid.Row="3" Orientation="Horizontal"  >
                                <TextBox  x:Name="ticketDiscountValue"  Width="200" PlaceholderText="Enter Amount"/>
                                <Button x:Name="submitTicketDiscount" Tapped="submitTicketDiscount_Tapped"  Content="Submit"></Button>
                            </StackPanel>
                        </Grid>
                    </ListBoxItem>

                    <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <controls:WrapPanel Background="Transparent"
                                        VerticalSpacing="5"
                                        HorizontalSpacing="0"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                </ListView>
                <Grid  x:Name="finalFinishColor" Grid.Row="3" Grid.ColumnSpan="6">
                    <Button x:Name="finalFinish" FontSize="20" Margin="0,10,0,10" Content="Finish" HorizontalAlignment="Center"   VerticalAlignment="Center" Click="finalFinish_Click" />
                    <Grid.Background>
                        <SolidColorBrush Color="Black" Opacity="0.7"/>
                    </Grid.Background>
                </Grid>

            </Grid>

            <Grid x:Name="finishTicketColor" Canvas.ZIndex="-1">
                <interactivity:Interaction.Behaviors>
                    <behaviors:Blur  Value="25" />
                </interactivity:Interaction.Behaviors>
                <Grid.Background>
                    <SolidColorBrush Color="Black" Opacity="0.23"/>
                </Grid.Background>
            </Grid>

        </Grid>

        <!--Print ticket -->
        <ScrollViewer Canvas.ZIndex="10" Width="0" Height="Auto" >
            <ScrollViewer x:Name="printInvoiceWrapper">
                <ScrollViewer x:Name="printMe" HorizontalAlignment="Left" Width="275" VerticalAlignment="Top" >
                    <Grid Padding="12,0,8,0">
                        <StackPanel>


                            <!-- ticket id-->
                            <Grid Background="Black" HorizontalAlignment="Stretch" Margin="60,0,60,10">
                                <TextBlock x:Name="printTicketNumber" Foreground="White" HorizontalAlignment="Center" FontSize="25" Text="50" />
                            </Grid>
                            
                            
                            <Rectangle x:Name="printCustomerTopBar"  Height="1" Margin="2"  Fill="Black" HorizontalAlignment="Stretch"/>
                            <!-- Customer Data-->
                            <TextBlock x:Name="printCustName" Foreground="Black" HorizontalAlignment="Left" FontSize="19"/>
                            <TextBlock x:Name="printPhone" Foreground="Black" HorizontalAlignment="Left" FontSize="15"/>

                            <!-- print Data time -->
                            <Rectangle Height="1" Margin="2"  Fill="Black" HorizontalAlignment="Stretch"/>
                            <TextBlock x:Name="printDateTime" HorizontalAlignment="Right" Foreground="Black" Margin="0,0,10,0"/>
                            
                            
                            <!-- print ticket Items-->
                            <Rectangle Height="1" Margin="2"  Fill="Black" HorizontalAlignment="Stretch"/>
                            <ListView x:Name="ticketListBoxPrtint" Grid.Row="1"  IsItemClickEnabled="True" LayoutUpdated="ticketListBox_LayoutUpdated" ItemsSource="{x:Bind CurrentTicket}" ItemTemplate="{StaticResource CurrentTicketDataTemplate}" Background="Transparent"  RequestedTheme="Light">
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="Margin" Value="0"/>
                                        <Setter Property="Padding" Value="0"/>
                                        <Setter Property="MinHeight" Value="20" />
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <ItemsStackPanel ItemsUpdatingScrollMode="KeepLastItemInView" />
                                    </ItemsPanelTemplate>
                                </ListView.ItemsPanel>
                            </ListView>
                            
                            
                            <!--tax, total -->
                            <Rectangle Height="1" Margin="2" Fill="Black" HorizontalAlignment="Stretch" />

                            <Grid  HorizontalAlignment="Stretch">
                                <TextBlock Text="Tax:" x:Name="printTaxTitle" Grid.Column="1"  Foreground="Black" />
                                <TextBlock x:Name="printTax" Foreground="Black" Grid.Column="2" HorizontalAlignment="Right" Text="$0.00" Margin="0,0,10,0" />
                            </Grid>
                            <Grid  HorizontalAlignment="Stretch">
                                <TextBlock Text="Total:"  Grid.Column="1" Foreground="Black" />
                                <TextBlock x:Name="printTotal" Grid.Column="2" Foreground="Black" HorizontalAlignment="Right" Text="$0.00" Margin="0,0,10,0" />
                            </Grid>

                            
                               <!--Payments -->
                            <Grid Margin="20,0,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="Tender:" x:Name="printPaymentTitle" Foreground="Black"/>

                                <ListView x:Name="printPaymentListView" Margin="0,0,10,0" Grid.Column="2" VerticalAlignment="Top" MinHeight="0" VerticalContentAlignment="Top"  ItemsSource="{x:Bind PrintTicketPayments}" HorizontalContentAlignment="Stretch" ItemTemplate="{StaticResource printPaymentListTemplate}" HorizontalAlignment="Stretch" RequestedTheme="Light" >
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="Margin" Value="0"/>
                                            <Setter Property="Padding" Value="0"/>
                                            <Setter Property="MinHeight" Value="10" />
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                </ListView>

                                

                            </Grid>
    
                            <!-- change given-->
                            <Grid x:Name="printChangeGrid">
                                <TextBlock Text="Change Given:" Foreground="Black" />
                                <TextBlock x:Name="printChange" Foreground="Black" HorizontalAlignment="Right" Text="$0.00" Margin="0,0,10,0" />
                            </Grid>

                            
                            <Grid x:Name="printAmountDueGrid" >
                                <TextBlock Text="Amount Due:" Foreground="Black" />
                                <TextBlock x:Name="printAmountDue" Foreground="Black" HorizontalAlignment="Right" Text="$0.00" Margin="0,0,10,0" />
                            </Grid>
                            
                            <!-- readdy Date-->
                            <Rectangle x:Name="readyDateBar"  Height="1" Margin="2" Fill="Black" HorizontalAlignment="Stretch"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="printReadyDateTitle" Text="Ready Date:" Foreground="Black"/>
                                <TextBlock x:Name="printReadyDate" Foreground="Black"/>
                            </StackPanel>


                            <!--Notes-->
                            <Rectangle Height="1" Margin="2" Fill="Black" HorizontalAlignment="Stretch"/>
                            <StackPanel>
                                <TextBlock x:Name="printNotesTitle" Text="Notes:" Foreground="Black" />
                                <ListView x:Name="printNoteListView"  Grid.Row="1" Grid.ColumnSpan="2" ItemsSource="{x:Bind ticketNotes}" ItemTemplate="{StaticResource PrintNoteTemplate}"  RequestedTheme="Light" >
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="BorderBrush" Value="White"/>
                                            <Setter Property="Margin" Value="0"/>
                                            <Setter Property="Padding" Value="0"/>
                                            <Setter Property="MinHeight" Value="10" />
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                </ListView>
                            </StackPanel>
                            
                            <!--Business Data -->
                            <StackPanel>
                                <TextBlock x:Name="businessName" Foreground="Black" HorizontalAlignment="Center" />
                                <TextBlock x:Name="businessPhone" Foreground="Black" HorizontalAlignment="Center" />
                                <TextBlock x:Name="businessAddress" Foreground="Black" HorizontalAlignment="Center" />
                                <TextBlock x:Name="businessAddress2" Foreground="Black" HorizontalAlignment="Center" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </ScrollViewer>
        </ScrollViewer>


    </Grid>
</Page>
